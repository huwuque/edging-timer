{
  "statusLine": {
    "type": "command",
    "command": "input=$(cat); echo \"$(echo \"$input\" | jq -r '.workspace.current_dir')  |  $(echo \"$input\" | jq -r '.model.display_name')  |  $(echo \"$input\" | jq -r '(.context_window.used_percentage // 0) | tostring + \"%\"')\""
  }
}
